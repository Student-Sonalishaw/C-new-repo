#include<stdio.h>
//Print sum of two numbers
int main(){
    int a , b;
    printf("Enter a and b: ",a,b);
    scanf("%d %d", &a,&b);
    int sum =(a+b);
    printf("Sum of two numbers are: %d\n",sum);

    return 0;
}